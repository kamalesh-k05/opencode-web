# Project instructions

## Workflow preferences

- Whenever building or previewing a website/app for the user, start the dev server and open the result directly in their default browser without asking or hesitating.
- Whenever we create a new website or portfolio project, push it to the user's GitHub account automatically. The user's GitHub username is `kamalesh05k` (account `kamalesh-k05`); existing repo example: `https://github.com/kamalesh-k05/opencode-web`. Ask the user for GitHub sign-in (device code flow) when a token is not available.
